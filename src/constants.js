/** Milliseconds per day */
const MS_PER_DAY = 24 * 60 * 60 * 1000;

/** Session cookie max age (7 days) */
const SESSION_MAX_AGE_MS = 7 * MS_PER_DAY;

module.exports = {
  MS_PER_DAY,
  SESSION_MAX_AGE_MS
};
